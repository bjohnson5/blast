# Models
To create a BLAST model:
- Define the model.json
- Implement the BLAST RPC protocol

model.json:
```
{
    "name": "name of model",
    "rpc": "address:port",
    "start": "start command"
}
```
